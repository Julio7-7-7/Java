package pfinsemestre;

import packageFormulario.JFrameBasal;

public class PFinSemestre {

    /*
    public static void quicksort(int V[], int izq, int der) {

  int pivote=V[izq]; // el primer elemento se usa como pivote
  int i=izq;         // i busca de izquierda a derecha
  int j=der;         // j busca de derecha a izquierda
  int aux;
 
  while(i < j){                          // mientras no se crucen las búsquedas                                   
     while(V[i] <= pivote && i < j) i++; // busca elemento mayor que pivote
     while(V[j] > pivote) j--;           // busca elemento menor que pivote
     if (i < j) {                        // si no se han cruzado                      
         aux= V[i];                      // los intercambia
         V[i]=V[j];
         V[j]=aux;
     }
   }
   
   V[izq]=V[j];      // se coloca el pivote en su lugar de forma que tendremos                                    
   V[j]=pivote;      // los menores a su izquierda y los mayores a su derecha
   
   if(izq < j-1)
      quicksort(V,izq,j-1);          // ordenamos subarray izquierdo
   if(j+1 < der)
      quicksort(V,j+1,der);          // ordenamos subarray derecho
   
}
    */
    
    
    public static void main(String[] args) {
        
    JFrameBasal P = new JFrameBasal();
    P.setVisible(true);
    
    /*cHombre h = new cHombre(80, 173, 19){
    };
    h.mostrar();
    System.out.println("Tu metabolismo basal es de "+h.metaBasal()+" Kcal\n");
    
    
    cMujer m = new cMujer(45, 165, 19){};
    m.mostrar();
    System.out.println("Tu metabolismo basal es de "+m.metaBasal()+" kcal");  
    */
    
    /*
    cCalculo F[];
       F = new cCalculo[5];
       F[0] = new cHombre(70, 150, 13);
       F[1] = new cMujer(50, 145, 19);
       F[2] = new cMujer(73, 156, 23);
       F[3] = new cHombre(80, 180, 25);
       F[4] = new cMujer(49, 190, 28);
    */
    
    
}
    
}
